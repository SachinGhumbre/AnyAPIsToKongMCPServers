# TraditionaAPIsToKongMCPServers
An enterprise-grade, full-stack automation platform that converts traditional REST APIs into AI-native Model Context Protocol (MCP) servers on Kong Gateway — accepting either an OpenAPI specification or an existing Kong proxy YAML as input, and producing Kong configurations with ai-mcp-proxy plugins on every route.
